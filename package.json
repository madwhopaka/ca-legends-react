{
  "name": "serv",
  "version": "1.0.0",
  "description": "server side for the react website",
  "main": "serv_index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client" : "cd client && npm install ", 
    "heroku-postbuild" :"npm run install-client && npm run buil",
    "start": "node serv_index.js",
    "dev": "nodemon serv_index.js"
  },
  "author": "Madwho",
  "license": "ISC",
   "engines" : {
     "node" : "15.12.0"
   }, 
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "install": "^0.13.0",
    "mongodb": "^4.1.1",
    "mongoose": "^6.0.5",
    "nodemon": "^2.0.12"
  }
}
